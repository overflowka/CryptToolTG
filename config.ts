const token = ""; // Telegram bot token
const key = ""; // generate key & iv with crypto library
const iv = ""; 
const users = [123456789, 012345678]; // userids

export { token, key, iv, users };